{
  "name": "@harmonic/job-queue",
  "version": "1.0.0",
  "description": "Redis-based job queue for Claude Code CLI analysis tasks",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "worker": "npx tsx src/worker.ts",
    "worker:dev": "npx tsx --watch src/worker.ts",
    "enqueue": "npx tsx src/cli-enqueue.ts",
    "dashboard": "npx tsx src/dashboard.ts",
    "health": "npx tsx src/health-check.ts"
  },
  "dependencies": {
    "bullmq": "^5.0.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
